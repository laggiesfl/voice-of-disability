# Navigation regression tests

`nav-app-link.test.mjs` guards the desktop navigation so the approved “Open Voice of Disability App” destination appears once as the prominent CTA rather than being duplicated in the text-link row.
